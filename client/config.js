import {
    API_KEY,
    AUTH_DOMAIN,
    MESSAGE_SENDER_ID,
    APP_ID,
    PROJECT_ID,
    STORAGE_BUCKET,
    API_URL
} from '@env'

export default {
    API_KEY,
    AUTH_DOMAIN,
    MESSAGE_SENDER_ID,
    APP_ID,
    PROJECT_ID,
    STORAGE_BUCKET,
    API_URL
}
